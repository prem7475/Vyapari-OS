# Vyapari OS API (Node.js + Express)

Express API with PostgreSQL, Firebase OTP bootstrap + JWT sessions, S3 document storage, Razorpay payments, and support chat.

Code lives under `services/api/server`. Start at `services/api/server/server.js`.
